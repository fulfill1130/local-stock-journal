# Public Release Checklist

Use this checklist before pushing or packaging a public repository.

## Must Not Be Tracked

- OAuth credentials, API keys, tokens, passwords, private keys, or `.env` files.
- Real profile state, holdings, transactions, watchlists, notes, or dividend income records.
- SQLite runtime databases, quote caches, generated market data, or local profile stores.
- Generated `demo_runtime/` files. Recreate them locally from `sample_data/` instead.
- Broker PDFs, screenshots, statements, spreadsheets, OCR output, or uploaded documents.
- Private IP addresses, private hostnames, local-only launchers, or machine-specific paths.
- Logs, backups, generated reports, downloaded binaries, temporary files, or cache files.

## Required Public Files

- `LICENSE`
- `README.md`
- `docs/SECURITY_AND_PRIVACY.md`
- `docs/DISCLAIMER.md`
- Synthetic sample data only under `sample_data/`

## Verification Commands

```powershell
git status --short
git ls-files | rg -i "(data/|profiles/|uploads/|output/|backups/|config/|\.sqlite|\.db|credential|token|secret|\.env|\.pdf|\.png|\.jpe?g|\.xlsx?|\.docx?|\.log)"
python -m unittest discover -s tests -v
git diff --check
```

The `git ls-files` check should return no private runtime data. Review any match manually before publishing.

## Current Demo Status

The repository includes deterministic synthetic sample market data and a synthetic demo profile generated by `python scripts/create_demo_data.py`.

`sample_data/` is safe synthetic fixture data intended for GitHub. `demo_runtime/` is generated by `python scripts/prepare_demo_runtime.py --reset`, ignored by Git, and should contain a `.demo_runtime` sentinel before it is deleted/recreated. `data/` is private real runtime data and must never be committed.

The read-only demo server can be started with `python src/main.py serve-demo` after `demo_runtime/` exists. It must stay isolated from real `data/` and must not enable Gmail, broker imports, external refresh, or background jobs.
